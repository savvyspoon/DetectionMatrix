// chart.min.js v4.4.1 - minimized version
// This is a placeholder for the actual Chart.js library
// In a real application, you would download the actual library from https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js
// or use a CDN link

(function(){
    console.log("Chart.js library loaded (placeholder)");
    window.Chart = function(ctx, config) {
        console.log("Chart created with config:", config);
        return {
            update: function() { console.log("Chart updated"); },
            destroy: function() { console.log("Chart destroyed"); },
            data: config.data || {},
            options: config.options || {}
        };
    };
    
    window.Chart.register = function() { console.log("Chart registered components"); };
    window.Chart.defaults = {
        color: '#666',
        font: { family: 'Helvetica', size: 12 }
    };
})();