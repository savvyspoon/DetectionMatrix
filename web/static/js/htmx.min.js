// htmx.min.js v1.9.10 - minimized version
// This is a placeholder for the actual htmx library
// In a real application, you would download the actual library from https://unpkg.com/htmx.org@1.9.10/dist/htmx.min.js
// or use a CDN link

(function(){
    console.log("HTMX library loaded (placeholder)");
    window.htmx = {
        on: function(event, callback) { console.log("HTMX registered event:", event); },
        trigger: function(element, event) { console.log("HTMX triggered event:", event); },
        ajax: function(method, url, config) { console.log("HTMX ajax call:", method, url); }
    };
})();